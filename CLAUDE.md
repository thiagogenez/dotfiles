# CLAUDE.md

The instructions for this repository live in [AGENTS.md](AGENTS.md). Read that file before
making any change.

It is kept as a single file so that every agent works from the same rules. This file exists
only so Claude Code finds them under its conventional name.
